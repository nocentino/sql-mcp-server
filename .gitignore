# Environment files
.env

# Node
node_modules/
sql-mcp-server/dist/
sql-mcp-server/package-lock.json

# Docker volumes
data/

# DAB files (dab-config.json itself is tracked)
dab-config.json.backup

# Certificates
certs/*.crt
certs/*.key

# macOS
.DS_Store
